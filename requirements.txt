# Core dependencies for Cognitive Tools framework
numpy>=1.21.0
scipy>=1.7.0

# Async support for LLM interfaces
asyncio-python>=3.8

# Optional LLM API integrations
openai>=1.0.0
anthropic>=0.8.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Data handling
pandas>=1.3.0

# Statistical analysis
matplotlib>=3.5.0
seaborn>=0.11.0

# JSON handling (built-in, listed for completeness)
# json (standard library)

# Type hints (Python 3.8+ built-in)  
# typing (standard library)

# Regular expressions (built-in)
# re (standard library)

# Mathematical operations (built-in)
# statistics (standard library)

# Dataclasses (Python 3.7+ built-in)
# dataclasses (standard library)

# Abstract base classes (built-in)
# abc (standard library)
